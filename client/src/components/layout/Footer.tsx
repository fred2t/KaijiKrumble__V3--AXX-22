interface Props {}

export default function Footer({}: Props): JSX.Element {
    return (
        <footer className="app-footer">
            <div className="large">👀</div>
        </footer>
    );
}
